import { SHomeContainer, SideBar, UpperBar } from "presentation";

const Home = (): JSX.Element => {
	return (
		<SHomeContainer>
			<SideBar />
			<UpperBar />
		</SHomeContainer>
	);
};

export default Home;
